import streamlit as st

def render_distribution(tracker):
    st.header("🎯 Distribution (Target Allocation)")
    st.info("Set target allocations (placeholder). This baseline keeps things simple.")
